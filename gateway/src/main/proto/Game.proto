syntax = "proto3";

package fr.rob.gateway.message.extension.game;

option java_outer_classname = "GameProto";

message Packet {
  string sender = 1;
  uint32 opcode = 2;
  bytes body = 4;
  uint64 createdAt = 5; // Unsafe input, change this ?
}

